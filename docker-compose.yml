services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: student-sqlserver
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "StudentDb@2026!"
    ports:
      - "1433:1433"
    volumes:
      - ./data:/var/opt/mssql
    # restart: unless-stopped

